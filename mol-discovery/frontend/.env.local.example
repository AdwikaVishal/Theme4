# Copy this file to .env.local and adjust if your backend runs on a different port.
# Do NOT include /api at the end — the API client adds that automatically.
NEXT_PUBLIC_API_URL=https://theme4.onrender.com
